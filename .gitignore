# byte-cache from reorder-candidates.py importing fcitx5_config.py
__pycache__/

# editor / OS cruft
*.swp
*~
.DS_Store
.qmlls.ini

# research/reference notes, not part of the packaged plugin
idea.txt
doc.txt
CLAUDE.md

# other frontend screenshots kept for reference; preview.png is the one
# published (the repo root is the marketplace's expected structure, so this
# keeps the other three out of it without deleting them)
bar-widget.png
virtual-board.png
change-page.png
